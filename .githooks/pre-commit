#!/usr/bin/env sh
set -eu

if ! command -v gitleaks >/dev/null 2>&1; then
  echo "gitleaks is required for commits. Install it from https://gitleaks.io/ and retry." >&2
  exit 1
fi

gitleaks protect --staged --redact
